modules:
  websocket_test_username_only:
    prober: websocket
    timeout: 5s
    websocket:
      http_config:
        basic_auth:
          username: "user"
  websocket_test_password_only:
    prober: websocket
    timeout: 5s
    websocket:
      http_config:
        basic_auth:
          password: "pass"

