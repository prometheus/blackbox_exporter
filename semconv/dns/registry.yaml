# yaml-language-server: $schema=https://raw.githubusercontent.com/open-telemetry/weaver/refs/heads/main/schemas/semconv.schema.json
groups:
  - id: registry.dns
    type: attribute_group
    brief: DNS attributes
    attributes:
      - id: phase
        type: string
        stability: stable
        brief: "DNS phase"
        examples: ["resolve", "connect", "request"]
